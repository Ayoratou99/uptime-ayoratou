# Screenshots

Drop PNG screenshots of this fork here and reference them from the main
README. Suggested names, matching the snippet in the README:

- `dashboard.png` — monitor list with a couple of monitors
- `status-page.png` — public status page with the redesigned header
- `users.png` — Settings > Users, showing roles and permission overrides
- `conditions.png` — the monitor condition builder on an HTTP monitor
- `incident-timeline.png` — an incident with several timeline updates

Upstream's screenshots are intentionally not reused, as they show the
original Uptime Kuma interface rather than this one.
